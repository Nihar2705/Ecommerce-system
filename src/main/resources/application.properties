# Server Configuration
server.port=8080

# MySQL Datasource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_inventory_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ------------------------------------------------------
# Added in Version 2: JWT Configuration
# ------------------------------------------------------
# Secret key used to sign access tokens (HS256). Replace with your own secret in production.
jwt.secret=8f42a73054b1749f8f58848be5e6502c8f42a73054b1749f8f58848be5e6502
# Access token validity: 1 hour (in milliseconds)
jwt.access-token-expiration-ms=3600000
# Refresh token validity: 7 days (in milliseconds)
jwt.refresh-token-expiration-ms=604800000
